package com.study.toy.item_14;

import static java.util.Comparator.*;

/*public class ComparatorTest implements Comparable<PhoneNumber> {
    private static final Comparator<PhoneNumber> COMPARATOR = comparingInt((PhoneNumber phoneNumber) -> pn.areaCode)
        .thenComparingInt((PhoneNumber phoneNumber) -> pn.prefix);

    @Override
    public int compareTo(PhoneNumber pn) {
        return COMPARATOR.compare(this, pn);
    }
}*/
